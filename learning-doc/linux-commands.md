| Command | Description | Shortcut / Notes |
|---------|------------|-----------------|
| ps aux | Show process status | a: all users, u: user info, x: not attached to terminal |
| ps aux | grep -i discord | -i: case insensitive, grep: search text using regex (Global Regular Expression Print) |
| pdfunite file1.pdf file2.pdf mergedfile.pdf | Merge PDF files | - |
| jupyter nbconvert --to pdf file.ipynb | Convert Jupyter notebook to PDF | - |
| mv file /path | Move or rename file | mv oldname newname |
| sudo apt install ./discord-*.deb | Install Discord .deb file | - |
| libreoffice --headless --convert-to doc yourfile.odt | converts odt to doc file | headless: doesn't open the doc as window, convertion happens in background|
| xdg-open . | opens the file as window  you are in |
| GIT COMMANDS | | 
| git config --global user.name "gitUserName"| - |
| git config --global user.email "gitemail@mail.com" | - |
| ssh-keygen -t ed25519 -C "mail@example.com" | generate ssh with ed25519 |
| git clone git@github.com:GITHUB_USERNAME/REPOSITORY.git | clone repository|
| git status | checks if the repository up to date |
| git add file.x | |
| git commit -m "message" | |
| git push | |
